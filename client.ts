// import { HOST, PORT } from "./common.ts";

// const conn = await Deno.connect({
//   hostname: HOST,
//   port: PORT,
//   transport: "tcp",
// });

// conn.write((new TextEncoder()).encode("hello, world!"));
// conn.close();
